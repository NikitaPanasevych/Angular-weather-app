import { PageBackgroundDirective } from './page-background.directive';

describe('PageBackgroundDirective', () => {
  it('should create an instance', () => {
    const directive = new PageBackgroundDirective();
    expect(directive).toBeTruthy();
  });
});
